<div class="container">
  <div class="row">
    <%= render "public/shared/error_messages", resource: resource %>
  </div>
  <div class="container">
    <div class="card card-container card-class">
      <img id="profile-img" class="profile-img-card" src="//ssl.gstatic.com/accounts/ui/avatar_2x.png" />
      <%= form_with model: @customer, url: customer_session_path do |f| %>
        <div class="form-group">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.password_field :password, autofocus: true, autocomplete: "current-password", placeholder: "Password", class: "form-control" %>
        </div>
        <div>
          <%= f.submit "ログイン", class: "btn btn-primary btn-block btn-signin" %>
        </div>
        <div class="mt-3">
          <%= link_to "Googleで登録", customer_google_oauth2_omniauth_authorize_path, method: :post, class: "btn btn-block btn-success btn-signin text-align-center" %>
        </div>
      <% end %>
      <p class="mt-3">
        <strong><%= render "public/shared/links" %></strong>
      </p>
    </div>
  </div>
</div>